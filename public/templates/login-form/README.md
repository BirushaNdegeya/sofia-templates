# Login Form

A centered sign-in screen built with HTML and CSS.

## How to use

1. Open `index.html` in a browser.
2. Change the heading, colors, and links in `index.html` and `css/style.css`.
3. Point the form `action` to your own authentication endpoint.
